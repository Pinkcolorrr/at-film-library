import { makeStyles } from '@material-ui/core';

export const searchFormStyles = makeStyles((theme) => ({
  searchForm: {
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    padding: theme.spacing(1, 2),
  },
}));
